<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Easing Functions -->
    <CubicEase x:Key="EaseOutQuad" EasingMode="EaseOut" />
    <CubicEase x:Key="EaseInOutQuad" EasingMode="EaseInOut" />
    <BackEase x:Key="EaseOutBack" EasingMode="EaseOut" Amplitude="0.3" />

    <!-- Page Transition Animation -->
    <Storyboard x:Key="PageTransitionIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.25"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                       From="-20"
                       To="0"
                       Duration="0:0:0.25"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <Storyboard x:Key="PageTransitionOut">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="1"
                       To="0"
                       Duration="0:0:0.25"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                       From="0"
                       To="20"
                       Duration="0:0:0.25"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Button Hover Animation -->
    <Storyboard x:Key="ButtonHoverEnter">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                       From="1"
                       To="1.02"
                       Duration="0:0:0.15"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                       From="1"
                       To="1.02"
                       Duration="0:0:0.15"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <Storyboard x:Key="ButtonHoverExit">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                       From="1.02"
                       To="1"
                       Duration="0:0:0.15"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                       From="1.02"
                       To="1"
                       Duration="0:0:0.15"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Card Hover Animation -->
    <Storyboard x:Key="CardHoverEnter">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                       From="0"
                       To="-4"
                       Duration="0:0:0.15"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0.95"
                       To="1"
                       Duration="0:0:0.15" />
    </Storyboard>

    <Storyboard x:Key="CardHoverExit">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                       From="-4"
                       To="0"
                       Duration="0:0:0.15"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="1"
                       To="0.95"
                       Duration="0:0:0.15" />
    </Storyboard>

    <!-- Sidebar Selection Animation -->
    <Storyboard x:Key="SidebarSelectionEnter">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.25"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Dialog Open Animation -->
    <Storyboard x:Key="DialogOpen">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.2"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                       From="0.9"
                       To="1"
                       Duration="0:0:0.2"
                       EasingFunction="{StaticResource EaseOutBack}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                       From="0.9"
                       To="1"
                       Duration="0:0:0.2"
                       EasingFunction="{StaticResource EaseOutBack}" />
    </Storyboard>

    <Storyboard x:Key="DialogClose">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="1"
                       To="0"
                       Duration="0:0:0.2"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                       From="1"
                       To="0.9"
                       Duration="0:0:0.2"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                       From="1"
                       To="0.9"
                       Duration="0:0:0.2"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Toast Notification Animation -->
    <Storyboard x:Key="ToastSlideIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                       From="50"
                       To="0"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutBack}" />
    </Storyboard>

    <Storyboard x:Key="ToastSlideOut">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="1"
                       To="0"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutQuad}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                       From="0"
                       To="50"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Loading Spinner Animation -->
    <Storyboard x:Key="SpinnerAnimation" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                       Storyboard.TargetProperty="Angle"
                       From="0"
                       To="360"
                       Duration="0:0:1"
                       EasingFunction="{StaticResource EaseInOutQuad}" />
    </Storyboard>

    <!-- Progress Bar Animation -->
    <Storyboard x:Key="ProgressBarAnimation">
        <DoubleAnimation Storyboard.TargetName="PART_Track"
                       Storyboard.TargetProperty="Width"
                       Duration="0:0:0.5"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Fade In Animation -->
    <Storyboard x:Key="FadeIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Fade Out Animation -->
    <Storyboard x:Key="FadeOut">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="1"
                       To="0"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Scale In Animation -->
    <Storyboard x:Key="ScaleIn">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                       From="0"
                       To="1"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutBack}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                       From="0"
                       To="1"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutBack}" />
    </Storyboard>

    <!-- Slide From Left Animation -->
    <Storyboard x:Key="SlideFromLeft">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                       From="-50"
                       To="0"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutBack}" />
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

    <!-- Slide From Right Animation -->
    <Storyboard x:Key="SlideFromRight">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                       From="50"
                       To="0"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutBack}" />
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.3"
                       EasingFunction="{StaticResource EaseOutQuad}" />
    </Storyboard>

</ResourceDictionary>
